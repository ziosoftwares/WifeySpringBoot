spring:
  datasource:
    url: jdbc:mysql://localhost:3306/project_wifey
    username: projectuser
    password: root123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  web:
    resources:
      add-mappings: false

  security:
    user:
      name: "user"
      password: "{noop}password"
  http:
    log-request-details=true:
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB

content:
  recipe: "content/recipe"

server:
  address: 192.168.0.21
  port: 8001

jwt:
  secret: "lailaMajnu@30035-extrasforlength"

s3:
  url: ${S3URL}
  bucket: ${S3BUCKET}
  keyid: ${S3KEYID}
  key: ${S3KEY}

logging:
  include-application-name: false
  level:
    root: INFO
    org.springframework.security.web.util.matcher: TRACE
    org.springframework.security.web.FilterChainProxy: TRACE
    org.springframework.web.servlet.DispatcherServlet: TRACE
    org.springframework.web.servlet.handler.AbstractHandlerMapping: TRACE
